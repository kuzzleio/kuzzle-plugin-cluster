{
  "apps": [{
      "name"         : "KuzzleServer",
      "script"       : "bin/kuzzle",
      "args"         : ["start"],
      "watch": [
        "./lib",
        "./{lib,config,bin}/**/*",
        "../kuzzle-plugin-cluster/lib",
        "../kuzzle-load-balancer/lib"
      ],
      "node_args": "--debug=7000"
    }]
}
